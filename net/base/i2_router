os.loadAPI("/disk/net/base/mac_address")

-- Loading switch
os.loadAPI("/disk/net/base/i2_switch")

-- Reading or creating MAC address
MAC = mac_address.get()

-- Creating NIC for the router
local NICManager = i2_switch.i2_nics
local routerNIC = NICManager.registerNIC("virtual_router", true)

-- TODO: router must have: DHCP (server / proxy), routing table rules, upstream/downstream port setup
function routerNIC:send(originNIC, packetType, source, nextHop, destination, packet)
     
end